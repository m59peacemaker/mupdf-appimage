name: update
on:
  push:
    branches:
      - master
  schedule:
    - cron: "*/5 * * * *"
jobs:
  update:
    runs-on: "ubuntu-20.04"
    steps:
      - uses: actions/checkout@v2
      - name: update
        run: ./scripts/foo.sh
